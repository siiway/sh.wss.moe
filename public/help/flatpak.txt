Flatpak + Flathub Setup

Usage:
  curl -fsSL sh.wss.moe/flatpak | sudo bash
  sudo bash <(curl -fsSL sh.wss.moe/flatpak)

Features:
  Installs Flatpak and adds Flathub repository

Requires: sudo
From: https://flatpak.org/setup/
Ref: https://wyf9.top/p/linux-newinst#flatpak-%E5%89%8D%E7%BD%AE
